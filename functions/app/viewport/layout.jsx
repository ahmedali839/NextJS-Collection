export const metadata = {
  title: "Viewport / generateViewPort",
  description: "Learning viewport(object) and generateViewPort(function)",
};

export default function RootLayout({ children }) {
  return (
    <div className="bg-yellow-300 text-black min-h-screen">{children}</div>
  );
}
